management:
  endpoints:
    web:
      exposure:
         include: "*"

spring:
  datasource:
    url: jdbc:h2:mem:test
    username: SA
    password: 12345678
    data: classpath:data.sql
    schema: classpath:schema.sql
